# Пины точные: воркер считает деньги клиента, и «поедет на свежих версиях»
# здесь не аргумент. Обновление — отдельным решением с прогоном на кадрах.
psycopg2-binary==2.9.10
numpy==1.26.4
opencv-python-headless==4.10.0.84
Pillow==11.0.0
torch==2.8.0
torchvision==0.23.0
open-image-models[onnx]==0.4.0
