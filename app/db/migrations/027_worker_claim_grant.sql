-- Воркер обязан МЕНЯТЬ строку задания, а не только читать её.
--
-- В 025 ему выдали select на render_jobs — этого хватает, чтобы задание
-- увидеть, и не хватает, чтобы его взять: app.claim_render_jobs переводит
-- строку в «running», ставит владельца и увеличивает счётчик попыток, то есть
-- делает update. После перевода функции на права вызывающего (026) отказ стал
-- виден честно: «permission denied for table render_jobs».
--
-- Отдельной миграцией, а не правкой 025: та уже накачена, и учёт миграций
-- сверяет контрольные суммы. Отредактированная задним числом миграция — это
-- расхождение между репозиторием и базой, и накат обязан на нём остановиться.
--
-- Delete не выдаётся: задания не удаляются никогда. Разбор упавших и
-- зависших — это смена статуса, а история попыток нужна, чтобы понимать, что
-- именно ломалось.
grant update on render_jobs to carswap_worker;
